{
  "name": "@burokku/example-layouts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "check": "pnpm build && cargo run --manifest-path Cargo.toml -- --check",
    "dev": "pnpm build && cargo run --manifest-path Cargo.toml"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vite": "8.1.5"
  }
}
