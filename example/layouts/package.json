{
  "name": "@burokku/example-layouts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "check": "pnpm --filter @burokku/runtime build && pnpm typecheck && pnpm build && cargo run --manifest-path Cargo.toml -- --check",
    "dev": "pnpm --filter @burokku/runtime build && pnpm build && cargo run --manifest-path Cargo.toml"
  },
  "dependencies": {
    "@burokku/runtime": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vite": "8.1.5"
  }
}
