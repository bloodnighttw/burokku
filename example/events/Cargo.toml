[package]
name = "burokku-example-events"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
burokku = { path = "../../crates/burokku", default-features = false, features = ["llrt-console"] }
