[workspace]
members = [
    "crates/runtime",
    "crates/winit",
    "crates/burokku"
]
resolver = "2"

# Performance counters are debug-only, including in optimized benchmarks.
[profile.bench]
debug-assertions = true
